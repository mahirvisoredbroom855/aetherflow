# ==============================
# AetherFlow Local Dev Config
# Copy this file to .env
# ==============================

# --- Database (Postgres) ---
# Used by API Gateway + Workers for persistence
DATABASE_URL=postgresql://aetherflow_user:dev_password_change_in_prod@localhost:5432/aetherflow_dev

# --- Redis (Optional) ---
# Used for caching/session storage if you enable it in services
REDIS_URL=redis://localhost:6379

# --- LocalStack (AWS Simulation) ---
# LocalStack gateway endpoint (S3 + SQS)
AWS_ENDPOINT_URL=http://localhost:4566
AWS_SQS_ENDPOINT=http://localhost:4566
AWS_S3_ENDPOINT=http://localhost:4566

AWS_DEFAULT_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# --- JWT / Auth ---
# Used by API Gateway to sign tokens (change later for real deploy)
JWT_SECRET=change-me-in-real-env

# --- App Defaults (MVP) ---
# These names should match what your services use when they create resources
S3_BUCKET_RAW=aetherflow-raw-uploads
SQS_QUEUE_ETL=aetherflow-etl-queue
SQS_QUEUE_ML=aetherflow-ml-queue
